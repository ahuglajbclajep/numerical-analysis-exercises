#ifndef SOLVE_LINEAR_EQUATION_SOR_UTIL_H
#define SOLVE_LINEAR_EQUATION_SOR_UTIL_H

#include <stdbool.h>

void show_input_matrix(int size, double [size][size]);
void show_answers_array(int size, double[size]);

bool all_mach(int size, bool [size]);

#endif
